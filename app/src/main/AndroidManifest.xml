<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.udecar">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CrearAuto"></activity>
        <activity android:name=".CrearLlantas" />
        <activity android:name=".CrearValvulas" />
        <activity android:name=".CrearFiltro" />
        <activity android:name=".CrearBujia" />
        <activity android:name=".CrearMotor" />
        <activity
            android:name=".VistaUsuario"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PrincipalAdmi" />
        <activity android:name=".RecuperarContra" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>